notifications:
  email: true

language: cpp
dist: bionic
compiler:
  - gcc

addons:
  apt:
    packages:
      - cmake
      - libsdl2-dev
      - libsdl2-ttf-dev
      - libsqlite3-dev
      - python-dev

script:
  - cmake CMakeLists.txt
